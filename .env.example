# ==================== SERVER CONFIGURATION ====================
PORT=3000
NODE_ENV=development

# ==================== SECURITY CONFIGURATION ====================
# JWT Secret - CRITICAL: Generate a strong random string for production
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Minimum 32 characters, recommended 64+ characters
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Session Configuration
JWT_EXPIRY=7d

# ==================== ADMIN ACCOUNT ====================
# Used for initial setup only
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Elaidi2025@

# ==================== CLOUDINARY CONFIGURATION ====================
# Image upload service - Required for lesson images
# Get credentials from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ==================== DATABASE CONFIGURATION ====================
# PostgreSQL (Production) - Leave empty for SQLite (Development)
# DATABASE_URL=postgresql://user:password@host:5432/database

# ==================== FRONTEND CONFIGURATION ====================
# CORS - Only needed if frontend and backend are on different domains
# FRONTEND_URL=http://localhost:3000
